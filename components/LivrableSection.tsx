"use client";

interface Props {
  projetId: string;
  statut: string;
  livrables: string[];
  isClient: boolean;
  isFreelance: boolean;
  hasAvis: boolean;
}

export default function LivrableSection({ projetId, statut, livrables, isClient, isFreelance, hasAvis }: Props) {
  return null;
}
